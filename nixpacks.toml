[phases.setup]
nixPkgs = ["ffmpeg", "nodejs_20", "python312", "python312Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt --break-system-packages"]

[phases.start]
cmd = "python bot.py"
